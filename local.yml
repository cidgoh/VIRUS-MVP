version: '3'

volumes:
  letsencrypt:

services:
  app:
    build:
      context: .
      dockerfile: ./dockerConf/app/Dockerfile
    image: covid_mvp_local_app
    ports:
      - "8050:8050"
    volumes:
      - .:/app
